#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', 'generate project')
  .command('build', 'new page')
  .parse(process.argv);